============
Installation
============

At the command line::

    $ easy_install openhub-django

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv openhub-django
    $ pip install openhub-django
